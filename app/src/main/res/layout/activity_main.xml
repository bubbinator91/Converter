<android.support.v4.widget.DrawerLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools"
	android:id="@+id/activity_main_layout"
	android:layout_width="match_parent"
	android:layout_height="match_parent"
	android:fitsSystemWindows="true"
	android:elevation="7dp"
	tools:context=".activities.MainActivity">

	<FrameLayout
		android:layout_width="match_parent"
		android:layout_height="match_parent"
		android:clickable="true">

		<FrameLayout
			android:id="@+id/container"
			android:layout_width="match_parent"
			android:layout_height="match_parent"/>

		<include
			android:id="@+id/toolbar"
			layout="@layout/util_toolbar"/>

	</FrameLayout>

	<com.bubbinator91.converter.util.ScrimInsetsFrameLayout
		xmlns:app="http://schemas.android.com/apk/res-auto"
		android:id="@+id/drawer_layout"
		android:layout_width="@dimen/navigation_drawer_width"
		android:layout_height="match_parent"
		android:layout_gravity="start"
		android:background="@color/navDrawerBackgroundColor"
		android:fitsSystemWindows="true"
		app:insetForeground="#4000">

		<RelativeLayout
			android:layout_width="match_parent"
			android:layout_height="match_parent">

			<RelativeLayout
				android:id="@+id/drawer_list_header"
				android:layout_width="match_parent"
				android:layout_height="@dimen/navigation_drawer_header_height"
				android:contentDescription="@string/activity_main_drawer_header_description"
				android:background="@drawable/header"/>

			<ListView
				android:id="@+id/drawer_list"
				android:layout_width="match_parent"
				android:layout_height="match_parent"
				android:layout_below="@+id/drawer_list_header"
				android:choiceMode="singleChoice"
				android:overScrollHeader="@color/colorAccent"
				android:divider="@null"
				android:scrollbars="none"/>

		</RelativeLayout>

	</com.bubbinator91.converter.util.ScrimInsetsFrameLayout>

</android.support.v4.widget.DrawerLayout>
